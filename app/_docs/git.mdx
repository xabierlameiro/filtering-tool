---
answers:
  [{ "value": "Sí", "isCorrect": false }, { "value": "No", "isCorrect": true }]
---

## ¿Creés que este componente es correcto?

```tsx
export default function Component({ dependency }) {
  return <div key={dependency.id}>{dependency.name}</div>;
}
```
